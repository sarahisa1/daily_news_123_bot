# 📊 일일 경제 뉴스 텔레그램 봇 (GitHub Actions 버전)

**서버 없이 무료로** 매일 아침 경제 뉴스를 텔레그램으로 받아볼 수 있습니다!

---

## ✨ 장점

- ✅ **완전 무료** - GitHub Actions 월 2,000분 무료
- ✅ **서버 불필요** - 컴퓨터를 켜둘 필요 없음
- ✅ **자동 실행** - 매일 지정 시간에 자동 발송
- ✅ **간편 설정** - 5분이면 완료

---

## 🚀 설정 방법 (총 5단계)

### 1단계: 텔레그램 봇 생성

1. 텔레그램에서 **@BotFather** 검색
2. `/newbot` 입력
3. 봇 이름 입력 (예: `나의경제뉴스봇`)
4. 봇 username 입력 (예: `my_news_12345_bot`)
5. **토큰 복사해서 메모** (예: `7123456789:AAHdqTcvCH1vGWJxfSe...`)

### 2단계: 채팅 ID 확인

1. 텔레그램에서 **@userinfobot** 검색
2. `/start` 입력
3. **Id 숫자 메모** (예: `123456789`)

### 3단계: 봇에게 /start 보내기

1. 1단계에서 만든 봇 검색
2. `/start` 입력
> ⚠️ 이 단계를 안 하면 봇이 메시지를 못 보냅니다!

### 4단계: GitHub 저장소 생성

1. [GitHub](https://github.com) 로그인
2. 우측 상단 **+** → **New repository**
3. Repository name: `daily-news-bot`
4. **Private** 선택 (토큰 보호)
5. **Create repository** 클릭

### 5단계: 파일 업로드 및 Secrets 설정

#### 5-1. 파일 업로드

1. 저장소 페이지에서 **Add file** → **Upload files**
2. 아래 파일들을 드래그&드롭:
   - `daily_news_bot.py`
   - `requirements.txt`
3. **Commit changes** 클릭

#### 5-2. 워크플로우 파일 생성

1. 저장소에서 **Add file** → **Create new file**
2. 파일명: `.github/workflows/daily_news.yml`
3. 아래 내용 복사 붙여넣기:

```yaml
name: 📊 Daily Economic News Bot

on:
  schedule:
    - cron: '0 23 * * *'  # 매일 한국시간 오전 8시
  workflow_dispatch:  # 수동 실행 버튼

jobs:
  send-news:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: pip install -r requirements.txt
      - run: python daily_news_bot.py --now
        env:
          BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
          CHAT_ID: ${{ secrets.CHAT_ID }}
```

4. **Commit changes** 클릭

#### 5-3. Secrets 설정 (중요!)

1. 저장소 → **Settings** 탭
2. 좌측 메뉴 **Secrets and variables** → **Actions**
3. **New repository secret** 클릭
4. 두 개의 Secret 추가:

| Name | Secret |
|------|--------|
| `BOT_TOKEN` | 1단계에서 받은 봇 토큰 |
| `CHAT_ID` | 2단계에서 확인한 ID |

---

## ✅ 테스트 방법

1. 저장소 → **Actions** 탭
2. 좌측에서 **📊 Daily Economic News Bot** 클릭
3. **Run workflow** → **Run workflow** 클릭
4. 텔레그램에서 뉴스 수신 확인!

---

## ⏰ 발송 시간 변경

`.github/workflows/daily_news.yml` 파일에서 cron 수정:

```yaml
schedule:
  - cron: '0 23 * * *'  # UTC 23:00 = 한국시간 08:00
```

**한국시간 변환표:**

| 한국시간 | cron 설정 |
|---------|----------|
| 오전 7시 | `0 22 * * *` |
| 오전 8시 | `0 23 * * *` |
| 오전 9시 | `0 0 * * *` |
| 저녁 6시 | `0 9 * * *` |
| 저녁 9시 | `0 12 * * *` |

---

## 📱 수신 메시지 예시

```
📰 일일 경제 뉴스 TOP10
📅 2026년 01월 30일 (Thu)

━━━━━━━━━━━━━━━━━━━━━━

📊 주요 시장 지수
  • KOSPI: 2,456.78
  • KOSDAQ: 756.43
  • 다우존스: 44,123.45

━━━━━━━━━━━━━━━━━━━━━━

💰 경제 뉴스 TOP5

1. 케빈 워시 연준 의장 공식 지명...
2. 한미 금리차 확대, 원화 약세 지속
3. ...

📈 국내 주식 뉴스 TOP5
...

🌍 해외 주식 뉴스 TOP5
...

🛢️ 원자재 뉴스 TOP5
...

₿ 암호화폐 뉴스 TOP5
...

━━━━━━━━━━━━━━━━━━━━━━
🤖 Powered by GitHub Actions
```

---

## 🔧 문제 해결

### Actions가 실행되지 않음
- 저장소가 60일간 활동이 없으면 자동 비활성화됨
- Settings → Actions → 활성화 확인

### "Unauthorized" 에러
- `BOT_TOKEN` Secret이 잘못됨
- BotFather에서 토큰 재확인

### "Chat not found" 에러
- `CHAT_ID` Secret이 잘못됨
- 봇에게 `/start` 보냈는지 확인

### 뉴스가 비어있음
- 네이버 웹페이지 구조 변경 가능
- Actions 로그에서 에러 확인

---

## 📂 파일 구조

```
daily-news-bot/
├── .github/
│   └── workflows/
│       └── daily_news.yml    ← GitHub Actions 설정
├── daily_news_bot.py         ← 메인 코드
├── requirements.txt          ← 패키지 목록
└── README.md
```

---

## 💡 추가 기능 아이디어

- [ ] 특정 키워드 필터링
- [ ] 관심 종목 알림
- [ ] 주말 제외 설정
- [ ] 여러 시간대 발송

---

**Made with ❤️ for Korean investors**
